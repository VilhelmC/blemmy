---
description: Never add Made-with Cursor commit trailer
alwaysApply: true
---

# Commit Message Policy

- Never add `Made-with: Cursor` (or similar tool attribution trailers) to
  commit messages.
- Keep commit messages focused on the change intent only.
- If a commit command would inject such a trailer, rewrite the commit message
  before finalizing the commit.
