# Playwright artifacts
playwright-report/
test-results/
.playwright/
# ── Build outputs ────────────────────────────────────────────
node_modules/
dist/
.astro/

# ── Environment ───────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── Local debug / exports (not tracked) ─────────────────────
debug/

# ── Personal data — never commit real CV content ──────────────
# Use src/data/cv-demo.json as the committed example.
# Keep your real CV files local or in a private repo.
src/data/cv-content.json
src/data/cv-*.json
!src/data/cv-demo.json

# ── Generated assets — reproduced by `npm run pdf` ────────────
public/*.pdf
public/*.jpg
public/*.jpeg
public/*.png
!public/blemmy.png

# ── Layout engine diagnostic reports (generated by test scripts) ─
docs/reports/*
!docs/reports/.gitkeep

# ── Local init playbook (not part of the published tree) ───────
REPO-INIT.md
v2.0.1-deployment-instructions.md
blemmy-v*-integration.md
*patch*-integration*.md
*integration*-patch*.md

# ── OS / editor ───────────────────────────────────────────────
.DS_Store
Thumbs.db
*.zip
patch/
.patch/
# Scratch trees (patch extraction, imports, zips, CI — safe to delete locally)
.tmp-*/
blemmy-v2.1-style-schema/
blemmy-v2.1-integration.md
.vscode/settings.json
